import 'package:flutter/material.dart';
class IzlemeListesi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('İzleme Listesi'),
    );
  }
}